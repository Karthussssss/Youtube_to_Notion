# Notion API credentials
NOTION_API_KEY=your_notion_integration_token_here
NOTION_DATABASE_ID=your_notion_database_id_here

# OpenAI API credentials
OPENAI_API_KEY=your_openai_api_key_here

# Application settings
DEFAULT_LANGUAGE=en  # Default language for transcripts (e.g., en, fr, es, de, ja)
PRESERVE_TEMP_FILES=false  # Set to "true" to keep temporary files for debugging

# The application uses gpt-4o-mini model by default
# You can change these settings in src/config/__init__.py if needed:
# - OPENAI_MODEL: Which model to use for summarization
# - MAX_SUMMARY_LENGTH: Maximum tokens for summary
# - MIN_SUMMARY_LENGTH: Minimum tokens for summary 